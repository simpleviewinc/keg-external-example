sync:
  tap:
    alpha: "/keg-external-example"
    beta: "docker://app"
    mode: "one-way-replica"
    ignore:
      vcs: true
      paths:
        - "/node_modules"
        - "/core/base/assets/*"
        - "/.*"
        - "!/.storybook"
        - "/container"
        - "!/.npmrc"
        - "*.lock"
        - "*.md"
        - "/temp"
        - "/web-build"
        - "/reports"
        - "/build"
        - "/docs"
